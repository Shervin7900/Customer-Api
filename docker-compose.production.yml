services:
  customerapi:
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
    ports:
      - "80:8080"
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 1G
